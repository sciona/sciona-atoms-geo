# Empty Expansion Inventory

This provider currently has no first-class expansion/refinement CDG operations.
The empty inventory is intentional until reusable geospatial deltas are mined
from validation results and promoted as shared operations.

